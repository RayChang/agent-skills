# Lessons — {{PROJECT_NAME}}

> Canon. Look up via `tasks/lessons.index.md` + `rg -i <keyword>` here; never broad-read.
> Before appending: `rg` for the same failure — if found, enrich that entry instead.
> Entry schema (all four fields mandatory):

<!-- Template entry — replace with the first real lesson, delete this comment:

## <imperative short title> (YYYY-MM-DD)
- Failure mode: <what went wrong — one sentence, past tense>
- Detection signal: <the exact error text / observable symptom>
- Prevention rule: <imperative and checkable — obeyable, not agreeable>
- Where: <file:line or command or area>
-->
