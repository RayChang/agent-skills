# Lessons Index

One line per lesson: `- <title> — <two grep keywords>`. Refresh whenever
`tasks/lessons.md` gains an entry. Compaction trigger: canon >48 KB or >50 entries →
PROPOSE a compaction (group entries by theme → one abstract principle entry per group ≥3
→ move originals to `tasks/lessons.archive.md`, never delete); propose only, don't run.

- (none yet)
