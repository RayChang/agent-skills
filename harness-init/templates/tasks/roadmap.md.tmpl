# {{PROJECT_NAME}} — Roadmap (canonical tracker)

> Canon. Sessions start from `roadmap.index.md`, then `rg -n '\bT<id>\b'` here and read
> only the matching section. Close-out updates this file FIRST, then refreshes the index.

## Tasks

<!-- One section per task. Format:
### T<id> <short title>
- [ ] status; on completion: [x] + date + how verified (test command + result)
- Spec/plan: tasks/T<id>-<slug>/
- Backlog (discovered during the task, not done): bullet list
-->

### T1 TODO(verify): first real task title
- [ ] not started

## Backlog (unordered)

<!-- Discovered work not yet promoted to a numbered task. -->
